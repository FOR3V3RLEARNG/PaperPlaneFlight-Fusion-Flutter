name: paper_plane_flight
publish_to: none
description: PAPER PLANE FLIGHT v2.2 Character Roster + Multiplayer — Flutter + Flame.
version: 2.2.0+22

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.38.0
  flutter_riverpod: ^3.4.2
  go_router: ^17.5.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/reference/
